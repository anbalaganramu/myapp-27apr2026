name: Test

on:
  push:
    branches:
      - master
  workflow_dispatch: {}  # Fixed here

jobs:
  log_message:
    runs-on: ubuntu-latest
    steps:
      - name: Log
        run: echo "The workflow is working!"